### [v1.26.9] - 2026-08-22

> Structured request hardening and removal of an obsolete WordPress import transport.

- **Dependency Maintenance**: Updated `@openrouter/sdk` from `1.2.54` to `1.2.57`, the complete direct and transitive TipTap family from `3.30.2` to `3.30.3`, and `@types/react-dom` from `19.2.4` to `19.2.5`. A normal npm install now resolves the aligned editor graph without peer conflicts, the compatible direct-package review is empty, and the registry audit reports zero vulnerabilities.
- **Structured Request Input Boundary**: Login, registration, page saving, comment mutations, legacy API actions, sitemap and RSS filters, and both owner-only OTA entry points now reject or safely normalize compound request values before they can reach PHP string functions, returning controlled client errors or bounded defaults instead of warning-backed 500 responses. Malformed authentication payloads consume the existing login or registration rate budget, registration lengths align with the installed user schema, and CSRF validation accepts only ordinary string tokens from headers, forms, or JSON while preserving the established source priority without echoing submitted token fragments.
- **WordPress Import Transport Cleanup**: Removed the unreachable stream-based remote-image helper. WordPress media import continues to require the existing bounded cURL path with per-hop public-IP validation, DNS pinning, redirect revalidation, TLS checks, and byte limits; no fallback can silently bypass those controls.
- **Uploads Shield Install And Repair Parity**: The packaged, freshly installed, integrity-audited, and repaired uploads `.htaccess` now share one versioned, case-insensitive deny contract. Script-like PHP variants plus common server-side and browser-executable extensions are denied consistently, directory listing remains disabled, and an older or incomplete shield is detected for repair instead of being accepted merely because it contains a marker or generic deny line.
- **Media Variant Filesystem Boundary**: Upload-relative media and generated-variant paths now strip URL query or fragment data before local resolution, normalize encoded path separators before traversal checks, and reject literal or percent-encoded dot segments plus control characters. Normal local, subfolder, and absolute CDN upload URLs continue to resolve while registry reads and variant probes stay confined to the uploads tree.
- **Runtime Environment And Credentialed Origin Boundary**: Generated configuration now defaults to production independently of request Host data, requires an explicit `VONCMS_ENV` development opt-in, and writes PHP diagnostics to a private per-install directory outside the server document root with a private temporary fallback. Root integrity checks require the sensitive-extension deny rule, while credentialed CORS now matches the complete scheme, host, and effective-port tuple instead of treating an omitted port as a wildcard.
- **Atomic Authentication Throttling And Public Query Bounds**: Login and registration now reserve attempts under one exclusive lock before credential work, closing parallel lost-update windows while using separate per-IP buckets so legitimate registration traffic cannot lock the login flow. Successful login retains its established bucket reset behavior. RSS offsets are conservatively capped, and out-of-range post sitemap chunks return an empty 404 XML response after a bounded count rather than issuing arbitrarily deep database offsets.
- **Early Public JSON Cache Hits And Coordinated Rebuilds**: Eligible anonymous post-list and settings requests now resolve a valid 60-second JSON cache response before loading `von_config.php`, PDO, timezone work, or schema probes. Cold misses release the PHP session before waiting, recheck after entering one shared rebuild/purge gate, and allow only the lock owner to write; invalidation uses the same gate so simultaneous expiry, rebuild, and purge cannot restore stale data. Lock-file failure remains fail-open through a live database read without an uncoordinated cache write, while response shapes, cache keys, TTL, cap, root/subfolder behavior, and mutation ownership remain unchanged.
- **Security Event And API Error Failure Boundaries**: The existing SecurityLogger call contract now revalidates PDO after optional configuration, strips query data from recorded endpoints, bounds scalar event, request, agent, severity, and detail fields, substitutes malformed UTF-8 safely, and contains every `Throwable` so telemetry cannot break the protected request. The central API error helper applies the same Throwable and malformed-message boundary while preserving generic guest 5xx responses, visible 4xx validation messages, and admin-only debug traces. No endpoint, database migration, role, or authentication rule was added.
- **Optional Node AI Authorization Parser**: Source-development AI routes now parse case-insensitive Bearer credentials with a linear space and tab scanner instead of an input-dependent regular expression. Valid `Authorization` and `x-ai-token` behavior, exact constant-time comparison, fail-closed configuration, and the existing route limiter remain unchanged.
- **Theme And Plugin Compatibility Specification**: The public extension guide now separates Required, Conditional, and Recommended rules; defines exact theme route, prop, settings, permalink, SSR/SEO, media, accessibility, and performance ownership; corrects the example VonSEO activation guard; documents system, custom HTML/CSS, and backend-integrated plugin boundaries; and adds root/subfolder, privacy, lifecycle, packaging, and multi-hop acceptance checks so a successful build alone cannot be presented as VonCMS compatibility.

### [v1.26.8] - 2026-08-20

> Bounded production-warning maintenance, reliable media discovery, persistent public search URLs, and crawlable theme navigation.

- **Dependency Maintenance**: Updated `@openrouter/sdk` from `1.2.32` to `1.2.54`; the complete direct and transitive TipTap family from `3.30.1` to `3.30.2`; `@vitejs/plugin-react` from `6.0.5` to `6.1.0`; Vite from `8.2.1` to `8.2.2`; `dompurify` from `3.4.13` to `3.4.14`; and `lucide-react` from `1.31.0` to `1.33.0`. The regenerated lockfile carries the compatible Rolldown `1.2.5` graph, the direct-package review is empty, and the npm registry audit reports zero vulnerabilities.
- **Adaptive AI Summary Quality**: The local no-API article summary now stays hidden for genuinely thin content, scales its maximum points from two to three before using the configured long-article limit, excludes non-narrative blocks, removes duplicate or near-duplicate sentences, blends bounded descriptive headings with ranked article sentences, keeps later headings eligible, and labels each extraction mode plus maximum points accurately.
- **Bounded AI Writing Editorial Recipe**: The basic Gemini writing assistant is now instructed to follow the requested or dominant content language, use standard Malaysian Malay when appropriate, treat pasted context as reference data rather than instructions, and respect its no-live-search boundary. News drafts keep supplied prompt and context facts as their boundary, preserve qualifiers and anonymity, avoid invented specifics and vague attribution, use exact quotations only when supplied, synthesize an original inverted-pyramid narrative, and avoid padding without importing the separate VonNewsRoom research or 15-section workflow.
- **Related Posts Ranking And Display Parity**: Bounded public candidates now include stored keywords so Category + Tags ranking works for guest direct loads instead of silently becoming category-only. Category and keyword comparisons ignore harmless case, repeated spacing, empty, and duplicate values; scheduled publish time drives recency, Most Recent sorting, and displayed dates; card-affecting changes invalidate memoized output; the excerpt toggle applies to Grid, List, and Cards; and the fresh-install title default stays aligned as `Related Posts`.
- **RSS Discovery Boundary**: The sitemap index now lists only its post and page sitemap documents. The RSS 2.0 endpoint, public route, and standard head `alternate` discovery link remain intact for readers and feed clients, avoiding duplicate sitemap-audit noise without removing syndication.
- **PHP 8.2 And Shared-Host Warning Maintenance**: Freshly generated configuration now handles null sanitizer input without a PHP deprecation, and new-post slug checks narrow a missing PDO row before reading it. The lightweight public JSON cache keeps fresh request-owned temporary files during pruning, removes only stale temporary files, and retains best-effort atomic writes without changing its 60-second TTL, 250-file cap, response format, or invalidation ownership.
- **System Image Upload Contract And Theme Cleanup**: General Settings now identifies Logo, Favicon, Social Share Large, and Social Share Square uploads explicitly so the server can enforce matching per-field extension and MIME pairs. Favicon accepts PNG plus both common ICO MIME aliases, SVG remains excluded, social images remain JPEG or PNG, and disguised mismatches fail closed while the existing authenticated system-upload, CSRF, 10 MB, original-file, and no-variant behavior stays intact. Theme saves remove only the confirmed unregistered `theme.classic` customization namespace while preserving all bundled and unknown custom-theme preferences.
- **Live Media Library Search**: TipTap Insert Media, Featured Image, and the Gallery manager now share a 300 ms bounded search contract. Typing and deleting refresh results automatically, an empty query restores the complete library immediately, explicit accessible clear controls work without Enter, pagination stays attached to the active query, and stale slower responses cannot replace newer results.
- **Persistent Public Search URLs**: Default, TechPress, Digest, and Prism keep public search in the `?search=` URL using bounded debounced replace semantics. Direct loads, hard reloads, bookmarks, and browser history restore the field and filtered results; clearing removes only search parameters while preserving category state. Search pages retain the existing `noindex, follow` contract, and Portfolio plus Corporate Pro remain unchanged because they do not expose the shared article-search UI.
- **Crawlable Public Navigation**: All six bundled themes render safe real `href` values for logo, configured menu, Home and Back, category, article ticker, and valid author-profile navigation while normal clicks retain the existing SPA handlers and modifier clicks retain browser behavior. Synthetic page and post IDs receive runtime-only public projections that are stripped before settings persistence; unresolved, draft, future, or missing targets remain buttons rather than inventing crawlable URLs. Overflow links and profile article cards retain their former full-width block layout, while hover-driven menus keep a continuous pointer plus keyboard-focus path across their visual spacing. Article cards retain canonical image or title anchors, fake hash links were removed, and dark mode, mobile menus, share, login, form, modal, load-more, and other actions remain buttons.
- **Integration Diagnostic Boundary**: The release config-isolation fixture passes only its required `NODE_PATH` into the child process, while security-shaped runtime checks report fixed test labels instead of replaying derived fixture or child-process output. CI failures remain attributable to their named contract without changing production runtime behavior.
- **Social Metadata Image Contract**: Public SSR and VonSEO now share one validated featured/profile, Social Share Large, Social Share Square, and logo priority chain with matching Twitter card kinds and JSON-LD output. Missing fresh-install favicon or social-image files no longer produce empty or nonexistent metadata, malformed image and preload URLs fail closed, and post saves reject only new or changed invalid featured-image URLs so unrelated edits can preserve an unchanged legacy value.
- **Homepage SSR Discovery Depth**: The lightweight homepage HTML seed, no-JavaScript article list, and CollectionPage ItemList now expose the latest ten published posts instead of five. React still applies each theme's existing Posts per page setting, while the bounded SSR query continues to calculate read time from `CHAR_LENGTH` without transferring full article bodies.
- **No-JavaScript Reading View**: Homepage, category, and single-content fallbacks now use one responsive editorial layout with bounded reading width, clear title hierarchy, semantic post paragraphs, article separation, a base-path-safe Back to Home control, keyboard-visible links, and phone spacing instead of inheriting Tailwind's unstyled reset. The fallback remains text-first and theme-neutral, adds no image requests, and does not change React rendering, routing, metadata, or indexing behavior.
- **Effective Publish-Time Parity**: Homepage seeds, bundled public listing cards, public profiles, Related Posts, hydrated and server article schema, and llms.txt now consistently prefer a due scheduled timestamp before falling back to creation time. SQL wall-clock dates and hydrated schema conversion stay independent of the visitor browser timezone, preventing day-boundary display drift and mismatched structured-data instants. Admin creation and edit history remain based on `createdAt` and `updatedAt`, while established date-based permalink paths deliberately retain their creation-date ownership to avoid moving indexed URLs.
- **Stable Page List Pagination**: The bounded Pages API now uses page ID as a deterministic secondary sort when multiple pages share the same creation timestamp, preventing equal-time rows from changing order between paginated requests without changing page visibility, search, payload, or scheduling behavior.
- **Scope Boundary**: Writer Submit for Review, `pending_review`, moderator review queues, review notifications, and published-content revision workflow move to the `v1.27` role and permission line; no editorial status, role, database, or guest-visibility contract is changed in this patch.

### [v1.26.7] - 2026-08-20

> Compatible dependency maintenance, a public-safe categories sidebar widget, resilient public theme colors, leaner admin runtime work, and authentication recovery closeout.

- **Dependency Maintenance**: Updated `@openrouter/sdk` from `1.2.20` to `1.2.32` and the complete direct and transitive TipTap family from `3.29.2` to `3.30.1`. The regenerated lockfile also carries the compatible Rolldown `1.2.4` graph, and the final npm registry audit reports zero vulnerabilities. This opening bump does not introduce editorial workflow or content-revision behavior yet.
- **Release Baseline And Changelog Maintenance**: Package metadata and maintained public docs now identify `v1.26.7` as the current release. The completed `v1.25.x` line is compressed into an archived summary, with full notes retained in Git history and release artifacts plus the long-lived OTA `.htaccess` and public-comment hydration regression markers preserved. Source packaging now excludes local `.vscode` workspace settings alongside private agent and planning files, with artifact regression coverage preventing those editor-only preferences from leaking into future Source ZIPs. Production builds exclude a live `von_config.php` before copying public files so configured development checkouts cannot replace the distributable sample, while integrity tools require Source-layout evidence before addressing a parent directory and reject marker-only managed blocks that have lost their routing or security directives.
- **Categories Sidebar Widget**: The primary-admin Widgets manager now offers a shared Categories block for Default, Digest, and TechPress. Supplied Trending, Categories, and Profile blocks become unavailable after their first placement, while Custom HTML/Text remains repeatable; existing duplicate rows stay editable and are not removed automatically. Categories shows ten public categories by default, supports a bounded 5/10/15/20 visible-item choice, and keeps any remainder behind a native Show more disclosure instead of restoring Digest's former noisy all-category pill strip. Category names remain dynamic rather than being copied into widget settings, so category rename and delete operations continue to use the existing category owner.
- **Public Category Link And Visibility Contract**: The settings response now exposes a runtime-only `publicCategories` projection derived from categories that currently contain public-visible, schedule-ready posts. Draft-only and saved-but-empty labels remain available to editors without generating empty public links, while the post save response compares the persisted category, status, and scheduled time before signalling a projection refresh in the active SPA session. Category and post mutations therefore refresh without making ordinary title or body autosaves repeat the settings request, and category-only widgets cannot reserve a blank sidebar column when no public category exists. Every rendered category remains a crawlable, encoded anchor that preserves root and subfolder installs, normal clicks reuse the existing SPA category handler, and modifier clicks or missing callbacks retain browser navigation. No category-count query, database table, endpoint, permalink mode, or alternate search flow was added.
- **Categories Widget Regression Guard**: Integration coverage locks the derived-only settings contract, non-persistence of runtime category data, bounded manager and Show more behavior, crawlable base-path-safe links, mutation refresh signals, the empty-sidebar boundary, and all four render locations across Default, Digest, and TechPress.
- **Category Navigation Scroll Reset**: Public category clicks now return the document to the top before applying either the homepage query update or a cross-route navigation. Query-only category changes no longer preserve the sidebar's deep scroll position, while the global path-based scroll behavior and browser-native modifier clicks remain unchanged.
- **Public Theme Color Resilience**: The first-render settings fallback now preserves the complete server-injected theme customization if the public settings request fails instead of rebuilding a generic blue theme. TechPress scopes its saved primary color across home, category, post, page, and profile descendants, while Digest scopes its saved accent across the same route families, keeping shared sidebar states aligned without leaking public colors into the admin ThemeEngine or changing the normal route-level color behavior of Default, Prism, Portfolio, and Corporate Pro.
- **TechPress Search Accent Control**: TechPress now gives the search field a fixed right-end primary-color segment instead of leaving its decorative search icon visually detached from the active theme. Contrast-aware icon color supports pale and dark custom colors, the same bounded segment becomes an accessible clear button when a query exists, input padding prevents text overlap, and focus treatment plus the existing character-limit notice remain intact across mobile and desktop.
- **Dashboard Timezone Clock**: The dashboard header now shows a compact 24-hour digital clock using the timezone already stored in General Settings, pauses its one-second timer while the tab is hidden, and falls back safely to UTC if an invalid identifier reaches the client. System readiness now sits naturally with the VonCMS and PHP metadata instead of crowding the clock, the mobile clock aligns with the title content, and admin or root users retain a neutral Change timezone shortcut to the existing Settings route without changing the five-card statistics grid, role boundaries, endpoints, or database.
- **Admin Runtime Concurrency And Monitoring Budget**: Dashboard comment counts, upload-folder scans, and analytics reads now release the authenticated PHP session after authorization so their parallel browser requests are no longer serialized behind one session lock. Media usage is fetched independently from traffic-period changes, preventing a 7/30-day chart switch from rescanning every upload. The timezone clock owns its one-second state inside a memoized leaf component instead of rerendering the complete dashboard and chart tree, while Security keeps its existing visible-tab, non-overlapping 30-second log polling but reuses aggregate chart statistics for one minute unless an owner explicitly refreshes or resets them. No Redis, table, endpoint, background worker, or guest-cache contract was added.
- **Admin Route-Aware Content Loading**: Restoring an authenticated session no longer fetches up to 200 posts and 200 full-body pages on every admin screen. Dashboard requests only its five recent posts, Discussion retains the post-title collection it currently needs, and Posts or Pages load only their own collection. Editor relies on its exact-item recovery instead of racing a bulk list against active typing, while Settings, Security, Gallery, Contact, Newsletter, Users, Widgets, Extensions, Database, plus public routes skip both collections. Independent in-flight and 5/200 coverage tracking coalesces duplicate work, retries one transient route failure, and invalidates pending results plus protected content and editor-recovery state when session identity changes. Save and delete mutations invalidate both affected coverage and any older matching request before updating local state, so a late response cannot restore stale ordering or a deleted item. Non-staff accounts cannot start these admin collection requests before their route redirect. Existing endpoint caps, server permissions, category fallback, and content-manager pagination remain unchanged.
- **Editor Text Color Save And Rendering**: Author-selected text colors, including dark and neutral choices, now pass directly through the shared editor HTML allowlist instead of being mistaken for foreign copy-paste residue during save. Cleanup of unwanted source-site colors remains isolated to paste handling. The shared public renderer also keeps that sanitized inline color through nested bold, links, and other typography marks in each independently sanitized content fragment, rather than allowing theme defaults to repaint child marks black. The color picker, sanitizer allowlists, theme palettes, and uncolored content remain unchanged.
- **Editor Object Lifecycle And Accessibility**: Filled tables now expose contextual row, column, and whole-table controls instead of requiring risky whole-document selection, while both UI attributes and the command handler enforce a 1 to 20 row and column boundary. Editor Media Library cards are named keyboard-focusable buttons, link editing can remove the current link, image and video tools expose explicit removal, and insertion dialogs retain invalid input with inline feedback instead of closing silently. Invalid video URLs keep keyboard focus inside the open dialog. Media searches ignore superseded responses, gallery alt-text sync failures no longer masquerade as full success, supported video labels match YouTube, TikTok, Instagram, and Facebook, and exact hard-load post recovery is reused by PostEditor instead of downloading the same post twice.
- **Editor Transition Reliability And Leaf Extraction**: Draft autosave now waits for a non-empty title instead of sending a request that the existing backend title contract must reject. Parent content echoes that already match the live TipTap HTML update the synchronization baseline without replaying `setContent`, preserving a single user-facing Undo step after deleting the only table while explicit `contentRevision` restores remain authoritative. Behavior-neutral toolbar primitives, image serialization, video embed parsing, and the content-preview modal now live behind focused editor support modules; shared video contracts keep pure parsing and serialization outside the TipTap dependency graph, while TipTap ownership, sanitization before insertion, selection state, media networking, source mode, and save orchestration remain in their established coordinators. Runtime regression coverage executes image-attribute escaping and supported video output instead of relying only on source markers.
- **Registration And Password Recovery Closeout**: Successful registration now clears username, email, and both password fields before replacing the form with a dedicated confirmation state that reports verification-email delivery and offers a direct return to sign in. Password and confirmation fields stack on narrow phones before returning to two columns at the small breakpoint. Registration and password-reset endpoints accept only `POST` plus preflight `OPTIONS`, unsupported reset methods advertise that contract through the standard `Allow` header, and reset emails target the real `/login?reset_token=...` route for configured-domain, root, and subfolder installs. Recovery requests use fixed 15-minute quotas for each normalized email and each IP-email pair, count accepted known and unknown addresses before account lookup, retain identical public success copy, and remain isolated from normal login and token submission. The long-standing auth honeypot now reaches Forgot Password requests and bounds repeated trap logging without revealing the trap response.
- **Profile Password Transaction Safety**: The shared profile endpoint now rejects non-string or excessively large password values before calling password functions, locks the credential row during a requested change, and commits the new hash, remember-token revocation, and profile fields as one transaction. A failed profile-field write therefore rolls the password change back instead of reporting failure after partially changing credentials, while successful changes retain the existing active-session revocation behavior across all six bundled themes.
- **Documentation Punctuation Cleanup**: Maintained README, changelog, and public documentation now use plain punctuation and ASCII hyphens consistently instead of em or en dashes. Integration coverage prevents those typographic separators from returning to shipped Markdown without changing technical meaning or release history.

### [v1.26.6] - 2026-08-09

> Editor legibility, explicit header identity modes, consistent public read time, public publish times, and compatible dependency maintenance.

- **Block Style Selector Legibility**: The Body/H1-H6 selector now uses intrinsic width with native-arrow clearance across phone, tablet, and desktop so the complete active label remains visible instead of clipping the final character. Live TipTap selection tracking and paragraph/heading commands remain unchanged.
- **Three Header Identity Modes**: General Settings now offers Logo + Text, Logo Only, and Text Only across all six bundled themes. Text Only hides an uploaded logo without deleting it, Logo Only falls back safely to the website name when no logo exists, and Logo + Text remains visible on Digest mobile. Existing sites without the new enum derive their mode from the legacy logo-title boolean, first-paint and no-JavaScript output honor the same state, and fresh installs seed both settings without rewriting upgraded databases. Custom themes that only read the legacy boolean retain their two older states but must adopt the shared identity resolver to represent Text Only while a logo remains stored.
- **Read Time Parity**: Homepage cards, public list API results, SSR seeds, and single-post payloads now use one raw-content character contract, so the same article no longer changes from 5 min read to 4 min read after opening it. The public list API and five-post homepage SSR queries calculate from `CHAR_LENGTH` without transferring full article bodies, while the shared metrics helper rejects direct web execution instead of exposing an empty indexable endpoint.
- **Visible Post Publish Time**: All six bundled single-post views now append a fixed 12-hour `AM/PM` publish time to the configured public date format in their existing metadata row. One shared resolver uses the scheduled publication timestamp when present and otherwise falls back to the original creation timestamp; the public post lookup, SSR seed, and injected React state preserve that timestamp across direct load, hard refresh, and SPA navigation. Zoned ISO values convert to the CMS timezone, while existing MySQL `DATETIME` values retain their CMS-local wall-clock meaning instead of shifting with the visitor browser; compact metadata rows wrap cleanly on narrow screens. Static pages, list cards, stored timestamps, schema dates, and modified-time semantics remain unchanged.
- **Scheduled RSS Publish-Time Parity**: RSS item ordering and `pubDate` now use the scheduled publication timestamp when present, with the original creation timestamp retained as the fallback for ordinary live posts. Future scheduled content remains excluded until its CMS-local publish cutoff, deterministic ID tie-breaking is preserved, and sitemap `lastmod` continues to represent modification time independently.
- **Editor Tool Dismissal Labels**: Compact formatting, image, and video tool panels now give their icon-only close controls matching hover titles and accessible names. The media close controls also use 44-pixel targets and declare button semantics explicitly, preventing accidental form submission if the editor shell is embedded in a form without changing dismissal behavior.
- **Dependency Maintenance**: Updated `lucide-react` from `1.30.0` to `1.31.0` and `@openrouter/sdk` from `1.2.18` to `1.2.20`; the refreshed compatible dependency review is empty and the registry audit reports zero vulnerabilities.

### [v1.26.5] - 2026-08-09

> Admin editor route context and practical publish-readiness maintenance.

- **Admin Editor Route Context**: Valid `/admin/editor` routes now derive their browser title from the requested content type and whether an existing ID is present, producing New Post, Edit Post, New Page, or Edit Page instead of inheriting the unknown-route Page Not Found title. New post/page URLs rebuild the shared empty-draft model after direct load or hard refresh instead of depending on volatile navigation state. Missing or invalid editor types are routed safely back to the relevant content manager, while genuinely unknown admin paths retain the accessible in-shell 404.
- **Editor Navigation Ownership**: Post editor routes keep Posts highlighted and page editor routes keep Pages highlighted without weakening segment-aware matching for invalid lookalike paths such as `/admin/posts1111111`.
- **Editor Preview Access**: Content Preview now has a first-level toolbar button on phone, tablet, and desktop instead of being hidden inside More Formatting. The existing sanitized preview modal and current HTML snapshot behavior remain unchanged.
- **Desktop Editor Essentials**: Desktop now keeps left, center, right, and justify alignment beside the other first-level formatting controls. The toolbar stays on one row whenever its writing card has enough room and wraps naturally only when the publishing sidebar leaves insufficient width, while the desktop image menu opens inward from its trigger so it remains inside the writing card instead of overlapping the publishing sidebar. More Formatting retains only Code Block and HTML Source on desktop, while phone and tablet keep the complete compact formatting and alignment panel.
- **Publish Readiness And Scheduled Save Truth**: The editor summary now covers title, body content, slug, excerpt or meta description, featured image, category, and scheduled date where those fields apply. Slug, excerpt or meta description, featured image, and category remain advisory; title and body remain publish requirements, while Schedule now stops before saving when its date and time are missing. The post API also returns its canonical persisted status and schedule so an invalid server-side schedule cannot remain labelled Scheduled in local state or produce a false success message.
- **Scheduler First-Run Reliability**: Opportunistic scheduled publishing now distinguishes an atomically created lock from an existing fresh lock, so the first request after a missing lock can publish due content immediately while concurrent and interval-throttled requests remain guarded. A transient database failure stays fail-open for the public page and leaves the marker eligible for retry instead of returning HTTP 500 or suppressing the next attempt. Direct web execution of the shared scheduler helper now fails with `403`, matching the SEO helper boundary, and the public bootstrap labels runtime helpers separately from the later maintenance-mode conditional.
- **PHP 8.2 Scheduler Compatibility**: The daily security-retention cutoff now creates its `DateTimeImmutable` before method chaining, preserving the documented PHP 8.2 baseline instead of relying on PHP 8.4-only unparenthesized object dereferencing that could make HTML routes return HTTP 500 on PHP 8.2 or 8.3 hosts.
- **Security Retention And Dashboard Budget**: Thirty-day security-log retention now runs through the traffic-driven maintenance scheduler at most once per day instead of issuing a `DELETE` after every recorded attack. Successful maintenance records the daily marker, while a transient failure leaves it stale so later traffic can retry. Manual maintenance reports the real deleted count, while dashboard refreshes cannot overlap, pause in hidden tabs, run every 30 seconds, release the PHP session before read queries, and combine the three summary counters into one aggregate query.
- **Authenticated Read Concurrency**: Authenticated settings, posts, pages, users, user totals, and security-log reads release the PHP session lock after authorization state is captured. Post and page collections then load in parallel without changing their established 200-item caps. App-owned settings requests share one in-flight fetch, login still queues the required protected refresh, and the editor no longer mounts a second settings hook solely to repeat the initial fetch. Existing sessions receive their CSRF token from the required authentication check, removing the separate eager CSRF boot request, and the public bootstrap avoids a duplicate timezone database lookup.
- **WordPress Bridge Batch Consistency**: Supported WXR posts, pages, and attachments now share one batch-offset model, while UI progress includes media items handled by the backend. One guarded reader helper owns XML cursor advancement so adjacent sibling items are processed exactly once instead of every alternating item being skipped by nested `next()` and `read()` calls. Browser checkpoints carry an explicit format version so an older offset model cannot resume against the new sequence. Strict random temp filenames remain inside the protected import directory, and scan/import work releases the authenticated session lock after primary-admin and CSRF checks so a long XML or media batch does not unnecessarily block other admin requests.
- **Hydrated Schema Ownership**: The server-rendered VonSEO JSON-LD script is now the single initial schema owner. React preserves that authoritative SSR graph on first hydration and reuses the same script element for later SPA navigation, preventing duplicate Organization, WebSite, and Article graphs from appearing in the hydrated document. Article breadcrumbs are emitted as their own `BreadcrumbList` graph node in both SSR and SPA output instead of assigning a WebPage-oriented breadcrumb property to `Article`.
- **Responsive Image Slot Accuracy**: Bundled homepage heroes, article headers, configurable grids, profile thumbnails, and Related Posts now advertise layout-specific `sizes` values instead of treating every image as a full viewport or generic card. New uploads can generate a 768-pixel intermediate candidate alongside 480, 960, and the bounded original, while the processor still refuses to upscale smaller source files. TechPress and Digest SSR preload metadata uses the same lead-image slot contract as React, Portfolio no longer preloads an image in hero modes that do not render one, the first visible Default and Prism card remains eager, and below-fold theme cards use lazy loading without changing original-image or lightbox access.
- **Optional Cron Operations Guide**: Existing install and API documentation now distinguish the built-in traffic scheduler from the optional cPanel cron trigger, document header-based `CRON_KEY` usage, cover subfolder URLs, and keep quiet-site scheduling optional rather than presenting cron as a normal requirement.
- **Dependency And Regression Review**: Updated `@openrouter/sdk` from `1.2.17` to `1.2.18`; the refreshed direct dependency review has no compatible update pending and the registry audit reports zero vulnerabilities. Integration coverage now locks valid editor titles and navigation ownership, blank new-editor hard-refresh reconstruction, first-level Preview and desktop alignment access, inward desktop image-menu containment, desktop More-panel code-tool ownership, the complete readiness checklist, server-confirmed scheduled-save state, the existing unknown-admin-route fallback, single hydrated JSON-LD ownership, scheduler first-run throttling and retry behavior, daily security retention and retry behavior, authenticated read concurrency, editor settings-fetch ownership, CSRF boot coalescing, adjacent WordPress WXR item iteration, batch consistency, responsive candidate parity, exact theme-slot mappings, SSR/React hero `sizes` parity, first-card priority, below-fold lazy loading, and direct helper denial; package metadata and maintained public documentation identify the v1.26.5 release line.

### [v1.26.4] - 2026-08-08

> Security dashboard visibility, Quick Editor exit safety, and public response-boundary maintenance.

- **Security Dashboard Chart Truth**: Threat Trends now zero-pads the seven-day range, renders every recorded event type instead of a stale three-event allowlist, and shows an explicit no-events state rather than an apparently broken blank chart. Distribution counts are normalized to finite numeric values before Recharts receives them, unknown event types receive stable fallback colors, and the real remember-token rotation conflict event is represented.
- **Quick Editor Exit Safety**: Public Quick Edit now uses the shared accessible admin modal layer with focus trapping, body scroll locking, focus restoration, and Escape handling. Title, content, publishing, SEO, featured-media, scheduling, and navigation changes are tracked against the latest saved snapshot; Close, Back, Escape, and Dashboard Editor handoff require confirmation before discarding an unsaved draft, while successful manual or automatic saves refresh the clean baseline.
- **Public SEO Response Helper Boundary**: Exact Redirect Manager resolution, redirect status normalization and emission, hit recording, and the complete HTTP 404 metadata response now live in a direct-access-guarded `seo_response_helper.php`. The public index remains the request orchestrator and HTML/hydration owner, while route parsing stays in `seo_route_helper.php` and content schema construction stays in `seo_schema_helper.php`; repeated branch-level 404 status/metadata writes and inline redirect SQL/target validation are removed from the HTML shell.
- **404 Response Consistency**: Missing post, page, profile, nested auth/setup, and unknown public routes now converge through one pre-head response function that preserves the real HTTP 404, missing-route canonical URL, `noindex, follow`, null schema, and React `not_found` hydration state. Exact `/login`, `/install`, and `/admin/*` shells remain valid, while root, subdomain, and subfolder deployments retain the same route classification without a second branch overwriting metadata.
- **Admin Route Fallback**: Authenticated unknown admin paths now render an accessible 404 state inside the existing admin shell instead of leaving its content area blank. Segment-aware menu matching prevents prefix collisions such as `/admin/posts1111111` from inheriting the Posts title or active navigation state, while valid nested editor routes keep their normal section ownership.
- **TechPress Short-Page Footer Containment**: The homepage, category, and search shell now uses the same column-flex ownership as TechPress page and profile views, allowing short or empty discovery content to fill the remaining viewport before the footer. Its centered category and search sections explicitly retain full container width inside the flex shell, preventing auto margins from collapsing them to content width. No-results searches and categories no longer leave unused page background below a naturally positioned footer, while long articles and mobile scrolling retain normal document flow; the standalone search field keeps a balanced 768-pixel desktop cap and slightly taller input.
- **Stable Organization Schema Identity**: PHP SSR and hydrated VonSEO now publish one shared `#organization` entity derived from the existing General Settings site name, Domain URL, and optional logo. The `WebSite` node and article publisher both reference that stable ID across root, subdomain, and subfolder installs; absent logos are omitted instead of emitting an empty `ImageObject`, homepage collection data remains a separate graph node, and no organization form, database field, or invented business detail is introduced.
- **Redirect Runtime Parity And Safety**: The integrated public redirect path and legacy compatibility engine now consume the same exact-match resolver instead of maintaining separate SQL, base-path, loop, status, and target checks. Local paths and explicit HTTP(S) destinations remain supported; API, asset, upload, and admin boundaries stay outside user redirects, invalid status values fall back to `301`, and scheme-relative or control-character targets fail closed before any hit is counted or Location header is emitted.
- **Adversarial Routing Review**: The response extraction was challenged against leaving all behavior inline, expanding the already substantial route helper, or moving the complete public controller out of `index.php`. A separate response helper was retained because it removes duplicated side effects without hiding database/content selection or the HTML shell; regression probes defend exact and nested routes, redirect loops, external target policy, root/subfolder normalization, direct-helper denial, pre-head metadata, hydration state, and Source/Deploy parity before release packaging.
- **Dependency Maintenance**: Updated every compatible direct package reported by the final dependency review: `@openrouter/sdk` from `1.2.4` to `1.2.17`; `dompurify` from `3.4.12` to `3.4.13`; `express-rate-limit` from `8.6.1` to `8.6.2`; `lucide-react` from `1.28.0` to `1.30.0`; `@types/node` from `26.1.2` to `26.2.0`; `postcss` from `8.5.25` to `8.5.26`; and `vite` from `8.2.0` to `8.2.1`. The refreshed lockfile carries matching Nano ID and Rolldown maintenance releases, the direct-package review is clean, and the registry audit reports zero vulnerabilities.
- **Regression And Release**: Integration locks numeric chart data, dynamic event series, honest empty states, shared Quick Editor modal behavior, unsaved-exit confirmation, centralized public 404 response ownership, the nested admin 404 fallback, TechPress short-discovery footer containment, stable Organization/WebSite identity, exact redirect lookup, protected route bypass, target validation, loop rejection, allowed status codes, hit-count timing, root/subfolder handling, guarded helper access, and Source/Deploy runtime parity. TypeScript, PHP lint, integration, formatting, build, package inventory, path safety, artifact parity, and SHA256 proof remain release gates for this patch.

### [v1.26.3] - 2026-08-02

> Canonical discovery, crawler routing, and metadata indexing maintenance.

- **Category URL Canonicalization**: Populated category discovery now resolves the stored category spelling before rendering. Case variants such as `?category=Sukan`, `?category=sukan`, and `?category=SUKAN` converge through a permanent redirect to one self-canonical URL instead of remaining separate indexable pages. The normal equality lookup retains the indexed category path, while a case-folded fallback covers installations using a case-sensitive database collation; category-plus-search results remain `noindex, follow`.
- **Exact Crawler Endpoint Routing**: The ultra-early `robots.txt`, `llms.txt`, sitemap, RSS, and feed interceptor now matches only the exact root or subfolder installation route. Nested lookalikes such as `/foo/robots.txt`, `/foo/sitemap.xml`, and `/foo/rss.xml` continue into the normal public 404 boundary instead of serving crawler documents with HTTP 200.
- **Utility And Search Indexing**: Server metadata marks login, admin, and installer shells `noindex, nofollow`, while public search results use `noindex, follow`. Hydrated React metadata preserves the search directive, and private shells omit homepage schema instead of inheriting indexable homepage signals.
- **Metadata Fallback Parity**: Post and page SSR descriptions now select the first non-empty manual meta description, excerpt, or content value, so a stored empty meta field cannot block useful fallback text. Hydrated pages now prioritize their manual `metaDescription` before excerpt or content, matching the server route.
- **Social Metadata Parity**: Raw SSR now emits explicit Twitter title, description, and image-alt metadata alongside the existing large-card image. Open Graph locale is derived only from Malaysian Malay or a validated region-qualified site language, local social images expose measured width and height, and article schema uses an `ImageObject` with those dimensions when available. When no content or configured social image exists, SSR now uses the configured site logo and omits image metadata if no real fallback exists instead of publishing a missing `/og-default.png`. Base schema state is initialized before optional database hydration so failed bootstrap or settings reads cannot produce an undefined-variable warning. Hydrated VonSEO preserves matching SSR dimensions while the social image is unchanged and drops stale dimensions on route changes without issuing a metadata-only image request or inventing locale/file values.
- **Canonical Path Cleanup**: Internal duplicate path separators are collapsed before the permanent canonical redirect is emitted, while the existing API/assets bypass and query-string preservation remain intact.
- **SEO Route Helper Boundary**: Crawler endpoint matching, discovery input normalization, category permalink/canonical lookup, shared public post lookup, SPA route classification, and description fallback now live in the direct-access-guarded `seo_route_helper.php`. Sitemap, RSS, llms, schema, and the public index share that boundary instead of maintaining duplicate helpers inside the complete HTML shell.
- **Regression And Release**: Integration now locks exact root/subfolder crawler matching, nested-route rejection, helper direct-access denial, category case canonicalization, private/search robots behavior, description fallback parity, social metadata detail, double-slash cleanup, and the shared route-helper boundary. TypeScript, PHP lint, integration, formatting, build, and Source/Deploy parity are release gates for this patch.

### [v1.26.2] - 2026-07-29

> Mobile-first publishing and complete admin viewport maintenance.

- **Mobile And Tablet Admin Shell**: The admin navigation now becomes a guarded off-canvas drawer below the desktop breakpoint, with route-change and Escape dismissal, body scroll locking, safe-area spacing, full-width content, and 44-pixel touch targets. Opening moves focus into the drawer, keyboard focus remains trapped until dismissal, the prior trigger regains focus afterward, and the closed drawer stays outside the mobile tab and accessibility order. Desktop sidebar collapse behavior remains separate, so phone and tablet navigation no longer inherits the narrow collapsed desktop rail.
- **Publish-Anywhere Editor Workflow**: Post and page editing now keeps Write, Publish, and AI panels in one mounted phone/tablet workflow and uses one compact non-scrolling TipTap toolbar on every viewport. The concise Body/H1-H6 selector keeps its native arrow clear on phones; desktop keeps Bold, Italic, Underline, text color, bullet list, numbered list, blockquote, link, video, table, and horizontal-line actions directly visible in one row at a readable icon scale, while Image retains its compact URL, Media Library, and device-upload balloon. Only alignment, code block, HTML source, and preview remain under More; phone and tablet retain the complete guarded Insert and More panels. Excerpt input typography now matches the compact Meta Description field instead of inheriting oversized textarea text. The toolbar uses a restrained slate surface for clearer separation from the writing canvas. Persistent safe-area Draft plus Publish/Schedule/Update actions remain at the bottom of the viewport, and the original save, autosave, scheduling, conflict, restore, authorization, upload, and HTML storage paths remain the only execution paths.
- **Editor Toolbar Trigger Reliability**: Body/H1-H6 now follows the live TipTap selection, so the same heading can be applied consecutively to separate blocks without selecting another style first. Shared toolbar buttons preserve the editor selection but defer actions until the completed click, preventing Insert or More from opening a nested action through the same pointer press. The color picker resets its native trigger after each application so the same color can be applied to a new selection repeatedly. When scrolling on phones and tablets, the formatting toolbar now reserves the exact height of the floating Write/Publish/AI section rail instead of occupying the same sticky position behind it; desktop retains its zero-offset toolbar. Link, image, video, table, list, quote, alignment, source, preview, undo, and redo handlers remain unchanged.
- **Responsive Admin Data Surfaces**: Posts, pages, users, newsletter subscribers, security events, contact forms, media, extensions, discussion, widgets, WordPress migration, settings, and database tools now avoid desktop-only width assumptions. High-density tables remain available at desktop widths while core mobile views use action-complete cards, bounded filters, readable forms, and tablet-safe column breakpoints.
- **Corporate Pro Tablet Footer Containment**: Corporate Pro now keeps its footer at two columns through tablet widths before returning to four columns on large desktops, while long custom contact email values wrap inside their bounded column instead of widening the public page.
- **Shared Admin Popup Layer**: Editor previews and insert dialogs, media pickers and lightbox details, audit history, extension/theme/plugin settings, redirects, OTA updates, discussion confirmation, user editing, and profile editing now share one body-level portal. The layer consistently applies a blurred backdrop, viewport and safe-area bounds, body scroll locking, initial focus, focus trapping, focus restoration, top-dialog Escape handling, and accessible dialog semantics.
- **Modal And Lightbox Safety**: Media lightbox navigation now has explicit touch-friendly Previous, Next, Close, and Edit details actions instead of depending on desktop double-click discovery. Nested redirect dialogs close only from the active top layer, and OTA progress cannot be dismissed through backdrop clicks or Escape while an update is running.
- **Modal Presentation Recovery**: The shared admin shell now preserves each dialog's intended centered maximum width instead of overriding theme, plugin, media, editor, user, and update dialogs with a nearly full-width desktop sheet. Admin dialogs and the public image lightbox use a dedicated 180ms opacity entrance with reduced-motion fallback instead of inheriting the global 500ms page transition; the shared admin backdrop keeps one lighter blur without changing focus, Escape, scroll-lock, or OTA dismissal guards.
- **Database And Settings Mobile Ergonomics**: Database Manager now bounds the table list below desktop width and guarantees a substantial phone/tablet SQL typing canvas instead of collapsing it to a single line between its toolbar and Run Query footer. System Settings exposes its destinations through one accessible native dropdown on phones, then returns to naturally wrapping tab buttons on tablet and desktop instead of hiding later destinations behind horizontal scrolling, while long TechPress toggle labels stack cleanly before returning to two columns.
- **Ads And Media Settings Mobile Ergonomics**: The In-Feed Ads header now stacks its title, frequency selector, and Native Placement badge on phones before returning to one row on larger viewports, preventing the control group from escaping its card. Media Settings replaces its hidden horizontal section strip with one native phone dropdown, a visible 3-by-2 tablet grid, and the original single desktop row; section state, save behavior, media tools, and backend paths remain unchanged.
- **Global Public Date Format**: General Settings now owns one validated public date-format choice with seven long, short, numeric, and ISO-style display options. The selected format and existing timezone are available during first paint and subsequent settings fetches, then apply consistently across every bundled theme's homepage, single-post, profile activity, and comment timestamps together with Sidebar date fallbacks and Related Posts, without a hydration-time date shift. Existing installations without the new key retain `July 29, 2026`-style output; database timestamps, admin operational dates, RSS, schema metadata, and date-based permalinks keep their required machine formats.
- **Settings Draft And Multi-Tab Ownership**: General Settings merges only fields changed inside its draft into the latest state available to that tab, while the canonical React settings writer now sends only top-level groups that differ from the tab's pre-save state. Saving Date Format in one tab and activating a theme in another can no longer replay an older full settings snapshot over the later action; theme, plugin, widget, newsletter, and customization writes retain their existing UI ownership, canonical endpoint, validation, secret handling, and rollback behavior.
- **Public Lightbox Accessibility**: The post-content image lightbox now identifies itself as a modal dialog, locks background scrolling, moves and traps focus, restores prior focus, and labels Close, Previous, and Next controls while preserving click, keyboard-arrow, Escape, and guarded swipe navigation.
- **Security And Ownership Boundaries**: Responsive work reuses the existing role-filtered actions and server endpoints without widening access. Primary-owner-only Security maintenance, Database Manager, media deletion, OTA, and protected user behavior remain guarded; the Security severity state/API filter now has its missing visible control, while the SQL inspector remains read-only.
- **Active Session Authorization Revocation**: Password login and remember-token restoration now bind each authenticated PHP session to the current database user ID, role, and password state. Authenticated and role-sensitive requests revalidate that fingerprint once per request, so deleting a user, changing a password, or changing a role removes stale session authority before it can reach owner-only OTA, database, settings, media, or user-management operations. The existing one-hour inactivity limit now also has a fixed 12-hour PHP-session lifetime; database outages fail closed without converting cached session roles into authorization.
- **Database Backup Identifier Safety**: Manual database exports and pre-import safety backups now quote metadata-derived table and column identifiers before querying or generating restore SQL, while control characters are removed from backup comment labels. Crafted schema names can no longer escape their identifier or comment context during a later owner-authorized backup, and the existing primary-owner, CSRF, import allowlist, confirmation, streaming, and byte-verification boundaries remain unchanged.
- **Dependency Maintenance**: Updated every compatible package reported by the final dependency review: `@openrouter/sdk` from `1.1.13` to `1.2.4`; `@tiptap/core`, `@tiptap/extension-image`, `@tiptap/extension-link`, `@tiptap/extension-table`, `@tiptap/extension-text-align`, `@tiptap/extension-text-style`, `@tiptap/pm`, `@tiptap/react`, and `@tiptap/starter-kit` from `3.29.1` to `3.29.2`; `lucide-react` from `1.27.0` to `1.28.0`; `postcss` from `8.5.24` to `8.5.25`; `@types/react` from `19.2.17` to `19.2.18`; `@types/react-dom` from `19.2.3` to `19.2.4`; `@vitejs/plugin-react` from `6.0.4` to `6.0.5`; and `vite` from `8.1.5` to `8.2.0`. The refreshed Vite lockfile includes its matching Rolldown and Lightning CSS graph, the final direct-package review is clean, and the registry audit reports zero vulnerabilities.
- **OTA Release Asset Replacement**: Staged OTA activation restores the original delete-first invariant for the shipped `assets/` directory by replacing it as one rollback-protected release unit alongside `docs/`. Retired fingerprinted JavaScript and CSS can no longer remain after an upgrade and be selected ahead of the current build, while an activation failure restores the complete previous asset directory with the rest of the release journal.
- **OTA JSON Response Integrity**: Temporary update cleanup now uses the updater's warning-suppressed recursive remover with bounded retries for transient Windows locks. A post-swap cleanup delay is logged without converting a completed update into a false failure, stale temporary files that cannot be cleared before a new update fail through the normal JSON result, and raw filesystem warning HTML can no longer corrupt the update response.
- **Regression And Release**: Integration locks the responsive shell including drawer focus entry, trapping, restoration, and closed tab-order exclusion; mobile publishing panels and safe-area actions; mobile data cards; the live-selection Body/H1-H6 trigger, completed-click overlay dispatch, repeatable color selection, collision-free stacked mobile/tablet sticky editor rails, readable compact icons, Excerpt/Meta Description typography parity, premium compact editor surface, direct desktop editorial/media essentials, the guarded Image balloon, advanced-only More grouping, and removal of the duplicate swipe/expanded toolbar paths; shared modal safety and sizing; fast reduced-motion-aware popup presentation; usable Database SQL canvas; visible Settings destinations; bounded In-Feed Ads controls; phone, tablet, and desktop Media Settings navigation; Corporate Pro tablet footer containment and contact wrapping; validated public date-format persistence, first-paint hydration, runtime output, timezone handling, homepage/single/profile/comment consumer parity, and rejection of raw date rendering; settings-draft ownership and changed-group persistence against multi-tab stale replay; accessible public lightbox behavior; popup adoption; non-dismissible OTA progress; rollback-protected complete replacement of release-managed assets and docs; warning-free deferred OTA cleanup and JSON response integrity; centralized authorization database bootstrap; preservation of anonymous CSRF state; and real endpoint proof that role changes, password changes, user deletion, and absolute expiry revoke active PHP-session authority before protected-page selection or stale comment identity reuse. TypeScript and the full integration smoke gate pass; package metadata and public docs identify `v1.26.2`.

### [v1.26.1] - 2026-07-28

> Word-safe discovery summaries and single-article schema parity maintenance.

- **Word-Safe Discovery Excerpts**: Homepage `CollectionPage` item descriptions and `<noscript>` article previews now keep complete words and Unicode code points within a 200-character ceiling, including the trailing `...`, instead of cutting summaries or emoji mid-character. Legacy encoded entities are decoded once before safe HTML output. Post Editor recommends 160-200 characters, shows a non-blocking warning after 220, and produces word-safe 200-character Auto Fill summaries. Server and hydrated route metadata also keep the existing 160-character SEO description boundary with the same behavior.
- **Article Schema Context**: Published post JSON-LD now aligns PHP SSR with hydrated VonSEO by declaring the canonical page through `mainEntityOfPage`, exposing the stored category as `articleSection`, and emitting a strictly validated primary `inLanguage` value from General Settings without hardcoding a portal locale. Invalid markup or punctuation is rejected consistently instead of being sanitized into different SSR and hydration values. Pages, permalinks, canonical URLs, breadcrumbs, routing, and media variants remain unchanged.
- **SEO Helper Boundary**: Server-side schema construction, 404 metadata, URL normalization, noscript text extraction, article-type validation, and language normalization now live in a dedicated guarded helper instead of expanding the public index bootstrap. Direct requests to the helper fail with `403`, while the public index is smaller than before this maintenance patch.
- **Category SSR Content Parity**: Populated `?category=...` discovery pages now server-render a bounded list of published posts from the requested category in both `<noscript>` output and the category `CollectionPage` `ItemList`, instead of exposing the homepage's latest five links until React hydration. Empty categories retain `noindex, follow`, while homepage boot data, public API behavior, permalinks, and hydrated theme rendering remain unchanged.
- **Fingerprint Asset Cache Boundary**: Apache and LiteSpeed installs now cache only fingerprinted `/assets/` build files for 30 days with `immutable`, matching the existing Nginx performance guidance without applying long-lived caching to HTML, PHP, APIs, crawler endpoints, uploads, or mutable public files. Fresh install and integrity repair generate the same bounded rule.
- **Portable Security Header Defaults**: Apache and LiteSpeed templates retain one-year HSTS for the active host without automatically opting every installation and subdomain into browser preload, while the deprecated `X-XSS-Protection` filter is removed. Operators who control HTTPS across every subdomain can still opt into `includeSubDomains` and preload explicitly at the server layer.
- **OTA Documentation Replacement**: OTA activation now replaces the shipped `docs/` directory as one rollback-protected release unit instead of overlaying individual files, so retired guides from older releases cannot remain beside the current consolidated documentation. Update packages must include the current docs directory; protected configuration, data, uploads, backups, and live `.htaccess` files remain untouched.
- **Primary Owner Update Discovery**: Dashboard update checks now normalize the primary administrator ID before applying the owner-only OTA boundary, restoring automatic GitHub release discovery when PHP returns database ID `1` as a number while appointed administrators remain excluded.
- **Dependency Maintenance**: Updated all compatible packages reported by the final dependency review: `@openrouter/sdk` from `1.1.8` to `1.1.13`; `@tiptap/core`, `@tiptap/extension-image`, `@tiptap/extension-link`, `@tiptap/extension-table`, `@tiptap/extension-text-align`, `@tiptap/extension-text-style`, `@tiptap/pm`, `@tiptap/react`, and `@tiptap/starter-kit` from `3.29.0` to `3.29.1`; `@types/node` from `26.1.1` to `26.1.2`; `express-rate-limit` from `8.6.0` to `8.6.1`; and `postcss` from `8.5.23` to `8.5.24`. The v1.26 compiler, router, and Tailwind baselines remain unchanged.
- **Regression And Release**: Integration executes the shared schema text and language normalizers, guards PHP and React schema parity plus the server helper boundary, keeps invalid language values out of JSON-LD, locks category-specific raw HTML and `ItemList` content against homepage-post drift, prevents the fingerprint cache rule from widening beyond immutable build assets, and runtime-tests complete docs replacement plus rollback; package metadata and public docs identify `v1.26.1`.

### [v1.26.0] - 2026-07-26

> After Hours opens the v1.26 line with compiler, styling, and bundled-extension baseline modernization.

- **After Hours Release Identity**: Package metadata and public docs now identify `v1.26.0 "After Hours"` as the current line. The installer uses the major-series `v1.26 "After Hours"` label, while all six bundled themes and six built-in plugins report version `1.26` in Extensions Manager.
- **Tailwind CSS 4 Migration**: Tailwind moves to the dedicated v4 PostCSS adapter and CSS-first configuration with explicit source boundaries matching the previous scan scope. The official migration rewrites legacy utilities to their v4 canonical equivalents across admin, editor, plugins, and bundled themes while removing the retired JavaScript config file; integration smoke now rejects retired color-opacity helpers.
- **TypeScript 7 Native Compiler**: Production typechecking now runs on the TypeScript 7 native compiler. A scoped TypeScript 6 compatibility alias remains available for current smoke tooling that still depends on the legacy Compiler API, avoiding a false choice between faster builds and working integration checks.
- **Dependency Baseline Refresh**: OpenRouter moves to SDK 1, the TipTap family moves together to `3.29.0`, and compatible `fs-extra`, Lucide, PostCSS, Recharts, and React Router 8 releases are aligned in the regenerated lockfile. Source development now declares the Node.js 22.22+ baseline required by React Router 8, while PHP production hosting remains unchanged.
- **Post Editor Maintenance Extraction**: `PostEditor.tsx` now delegates edit-history presentation, SEO controls and score presentation, the featured-media picker modal, and shared text extraction to focused editor modules. Save, autosave, conflict handling, full-content restore, AI actions, media requests, and HTML storage remain owned by the existing controller paths.
- **Article Schema Selection**: VonSEO now offers `Article`, `NewsArticle`, and `BlogPosting` for published post JSON-LD while retaining `Article` as the fresh-install, legacy, and invalid-value fallback. PHP SSR, homepage item metadata, and hydrated React output use the same validated choice without changing pages, permalinks, canonical URLs, Open Graph types, redirects, sitemap, or routing.
- **Public Page Navigation Cleanup**: Page links resolved by the public shell now carry a slug-scoped route hint so SPA navigation fetches the full page directly instead of first producing an expected post-endpoint 404. Direct loads and genuinely ambiguous slugs retain the existing server metadata and dual page/post lookup fallback.
- **Default Card Image Ratio**: Default theme homepage cards now keep a consistent 16:9 thumbnail frame across mobile, tablet, and desktop while preserving responsive image sources and cover cropping.
- **Default Public Feed Stability**: Default theme now memoizes its published-post projection so ticker synchronization cannot repeatedly reset and refetch the homepage list, preventing continuous height flicker around the Load More area.
- **Dashboard Media Usage Clarity**: The Dashboard now labels upload-folder consumption as `Media Usage` and shows its real formatted size instead of a synthetic percentage that could be mistaken for the hosting account quota.
- **Dashboard Information Density**: The oversized system banner is replaced by a compact version/status identity, summary cards use a shorter horizontal layout, Visitor Traffic consumes less vertical space, and Recent Activity plus its history modal use full article titles, readable local timestamps, and honest activity wording instead of simulated audit-log attribution.
- **Dashboard Traffic Detail**: Visitor Traffic now uses the existing analytics response to show total visits, unique visitors, active days, a combined daily visits/unique-visitors chart, and a permalink-neutral period summary with daily average, peak day, and peak visits. Raw historical paths are no longer ranked as current pages after permalink changes, and no analytics table, column, migration, or additional endpoint is introduced.
- **Admin Brand Mark Alignment**: The admin sidebar now uses a white geometric SVG interpretation of the official VonCMS circle, `V`, and rising-arrow mark on the existing 40×40 responsive grid, while public-theme fallback branding retains its established color treatment.
- **Security Source Ranking Clarity**: Security Dashboard replaces the oversized full-width source-IP bar chart with an auto-height ranked list, bounded relative indicators, explicit localhost context, and neutral `Most Active Source` wording so recorded failures are not automatically presented as confirmed attackers.
- **Content Manager Edit Visibility**: Post and page lists now show a compact `Edited` date beneath the existing `Created` date when stored update metadata is newer, preserving the current table width while exposing the full update timestamp through the accessible label and desktop tooltip.
- **Content Manager Narrow-Width Clarity**: Long category and author values remain contained in their fixed columns, category tooltips now retain the full filter value, and `Publish At` uses a compact date/time label with the complete timestamp available to assistive technology and desktop hover.
- **Regression And Release**: Integration smoke now recognizes the After Hours documentation and `v1.26.x` routing labels while retaining the existing release, editor, public-theme, extension, security, and PHP contracts. Package creation continues to derive Deploy and Source artifact names from the canonical package version.

### Archived v1.25.x Summary

> The completed OpenGate line is compressed to keep the root changelog focused on active v1.26.x development. Detailed notes remain available through repository history and archived release artifacts.

- **v1.25.13 OpenGate closeout**: Public theme startup, category handoff, and legacy sidebar compatibility.
- **v1.25.12**: Guest homepage ticker continuity, slow category-navigation feedback, and adaptive empty-sidebar layouts.
- **v1.25.11**: Media Library safety, extension integrity, responsive-variant maintenance, recovery/import hardening, OTA rollback, and owner parity.
- **v1.25.10**: Settings ownership, database safety, embedded-runtime isolation, URL/ad/audit boundaries, and bounded administrative exports.
- **v1.25.9**: Profile polish, dedicated remember-token hardening, token lifecycle parity, and noscript/editor regression coverage.
- **v1.25.8**: SEO crawler polish for site-name and category discovery pages, canonical public identity, and bundled-theme metadata alignment.
- **v1.25.7**: Related Posts Most Viewed data correctness, settings/render parity, and deterministic candidate handling.
- **v1.25.6**: Related Posts direct-link recovery after the lean public boot cleanup.
- **v1.25.5**: OpenGate security hardening, lean public discovery, theme/editor polish, and the **Public Comment Hydration Note** correctness guard.
- **v1.25.4**: Responsive third-party ad handling, editor/media polish, cache-control maintenance, and 30-day generated `.htaccess` asset caching.
- **v1.25.3**: Lightweight public-shaped JSON caching for repeat posts/settings reads, cache invalidation, versioned robots policy, and host-safe cache storage.
- **v1.25.2**: Public JSON-cache preparation, maintenance-response handling, UI consistency, and root/subfolder deployment parity.
- **v1.25.1**: First public-source impression, sidebar reading context, release-package truth, open-source contribution readiness, and install-doc alignment.
- **v1.25.0 OpenGate foundation**: Direct-entry routing, loading and AI completion fixes, local font delivery, security hardening, open-source onboarding, and the **OTA `.htaccess` Upgrade Warning** requiring one System Tools repair after OTA from v1.24.x.

### Older Releases

> HourGlass and earlier release history is compressed to keep the GitHub changelog focused on the active v1.26+ lines. Detailed notes remain available through repository history and archived release artifacts.

- **v1.24.11 HourGlass closeout**: Slow-network profile loading stability, dashboard count truth, AI crawler policy, Nginx deployment guidance, and Deploy-package parity.
- **v1.24.10**: Current comment-avatar fallback, managed .htaccess packaging proof, and SSR article/profile schema URL repair.
- **v1.24.9**: Durable TipTap image state, bounded search, OTA redirect handling, readiness-based skeletons, and open-source preflight proof.
- **v1.24.8**: Profile activity beyond preload limits, appointed-admin secret isolation, and public/profile/editor privacy closeout.
- **v1.24.7**: Built-in extension runtime parity for campaigns, SEO, analytics, AI summaries, gifts, promo bars, and related posts.
- **v1.24.6**: Category and search first-paint loading parity across bundled public themes.
- **v1.24.5**: Editor-engine maintenance extraction while preserving HTML storage, toolbar, media parsing, and save behavior.
- **v1.24.4**: Public interaction polish, comments-off first paint, editor video tools, import guardrails, and theme preload cleanup.
- **v1.24.3**: Dashboard count truth, public discovery beyond capped preloads, and release alignment.
- **v1.24.2**: Theme search stability, profile 404 behavior, responsive admin search, and editor styling parity.
- **v1.24.1**: TipTap stabilization for autosave, restore freshness, toolbar behavior, paste cleanup, navigation, and regression coverage.
- **v1.24.0 HourGlass foundation**: TipTap editor activation, content/SEO cleanup, escalating login penalties, and bundle-size optimization.

- **v1.23.10 Rentaka closeout**: API key privacy/rotation, media fallback reporting, Page Manager search parity, vertical video embed handling, scanner polish, form accessibility cleanup, and v1.24.x transition wording.
- **v1.23.9**: PHP 8.5 compatibility and static-analysis cleanup across importer, backup/import, public helpers, installer, repair, and security metadata surfaces.
- **v1.23.8**: CI/CD scanner-noise cleanup, sanitized HTML render guardrails, packaged README license-link alignment, and benchmark-copy cleanup.
- **v1.23.7**: WordPress importer remote-fetch hardening, redirect/DNS/IP validation, read-time alignment, and settings mirror hardening.
- **v1.23.6**: Database Manager restore/backup clarity, import restore fixes, public theme performance polish, settings and repair alignment, and roadmap pull-forward work.
- **v1.23.5 to v1.23.0 Rentaka line**: Security audit hardening, autosave/save feedback polish, promo bar color controls, build chunk optimization, release promotion, Kirana-to-Rentaka consolidation, and Content Manager alignment.
- **v1.22.x Kirana line**: Hybrid decoupled CMS stabilization, RSS and avatar fixes, manual excerpt/meta preservation, discussion/search/admin flow improvements, routing/path robustness, and broad publishing workflow polish.
- **v1.21.x Breeze line**: Managed `.htaccess` strategy, responsive image rollout, scheduler/security hardening, editor stability, packaging realignment, and performance/UI refinement.
- **v1.20.x Mandala line**: Universal path agnosticism, TechPress polish, SPA ad intelligence, engagement/discovery updates, image SEO engine, and early security hardening.
- **v1.11.x Nara foundation**: Core routing, hydration, SEO, security, mail, IndexNow, and early hybrid architecture work that formed the base for later release lines.

For forensic history before `v1.26.x`, use repository history or archived release artifacts instead of expanding this root changelog again.
